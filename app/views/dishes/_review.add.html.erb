<%= simple_form_for([@dish, @dish.reviews.build]) do |f| %>
  <%= f.input :name %>
  <%= f.input :body %>
  <%= f.input :star, :collection => ["1","2","3","4","5"] %>
  <%= f.button :submit, class: 'btn btn-success' %>
<% end %>
